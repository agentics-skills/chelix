# General UI Rules

## Business Requirements

1. The UI MUST obtain every backend-owned value from the backend and MUST NOT duplicate, derive, redefine, or synthesize that value.
2. Every failure MUST produce an explicit error and follow documented business logic; silent fallbacks are prohibited.

